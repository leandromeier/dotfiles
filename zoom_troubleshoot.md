##

1. ping zoom.us to see if zoom can reach its domains
2. If yes, clear zoom config: in bash 'rm -r ~/.zoom'
3. maybe un- and re-install zoom: this is 'yay -Rns zoom', and maybe also manually delete zoom config files in .config or wherever else you find some (before re-installing)
